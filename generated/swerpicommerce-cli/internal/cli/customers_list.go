// Copyright 2026 Vincenzo Vigorito and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCustomersListCmd(flags *rootFlags) *cobra.Command {
	var flagEmail string
	var flagDataInizio string
	var flagDataFine string
	var flagModifiedAfter string
	var flagSort string
	var flagLimit int
	var flagOffset string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "`data_inizio` / `data_fine` filtrano sulla **data di registrazione** (`data_creazione`)",
		Example:     "  swerpicommerce-pp-cli customers list",
		Annotations: map[string]string{"pp:endpoint": "customers.list", "pp:method": "GET", "pp:path": "/customers", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"id", "-id", "cognome", "-cognome", "data_creazione", "-data_creazione", "ultima_modifica", "-ultima_modifica"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			path := "/customers"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "customers", path, retainCLIQueryParams(cmd, map[string]string{
				"email":          formatCLIParamValue(flagEmail),
				"data_inizio":    formatCLIParamValue(flagDataInizio),
				"data_fine":      formatCLIParamValue(flagDataFine),
				"modified_after": formatCLIParamValue(flagModifiedAfter),
				"sort":           formatCLIParamValue(flagSort),
				"limit":          formatCLIParamValue(flagLimit),
				"offset":         formatCLIParamValue(flagOffset),
			}, map[string][]string{"email": {"email"}, "data_inizio": {"data-inizio"}, "data_fine": {"data-fine"}, "modified_after": {"modified-after"}, "sort": {"sort"}, "limit": {"limit"}, "offset": {"offset"}}, "offset", "offset"), nil, flagAll, "offset", "offset", "limit", 100, "", "", "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				var selectErr error
				filtered := data
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"email": true, "id": true, "lista_email_id": true, "listino_id": true, "stripe_id": true, "user_id": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				if err := printOutput(cmd.OutOrStdout(), wrapped, true); err != nil {
					return err
				}
				return selectErr
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"email": true, "id": true, "lista_email_id": true, "listino_id": true, "stripe_id": true, "user_id": true})
		},
	}
	cmd.Flags().StringVar(&flagEmail, "email", "", "Filtra per email esatta")
	cmd.Flags().StringVar(&flagDataInizio, "data-inizio", "", "Data di creazione minima (inclusiva). Data secca `YYYY-MM-DD` o date-time ISO 8601.")
	cmd.Flags().StringVar(&flagDataFine, "data-fine", "", "Data di creazione massima (inclusiva). Una data secca `YYYY-MM-DD` copre l'intera giornata.")
	cmd.Flags().StringVar(&flagModifiedAfter, "modified-after", "", "Solo i record modificati **dopo** questo istante (esclusivo).")
	cmd.Flags().StringVar(&flagSort, "sort", "-id", "Campo di ordinamento, prefisso `-` per il decrescente. (one of: id, -id, cognome, -cognome, data_creazione, -data_creazione, ultima_modifica, -ultima_modifica)")
	cmd.Flags().IntVar(&flagLimit, "limit", 100, "Numero massimo di risultati (default 100)")
	cmd.Flags().StringVar(&flagOffset, "offset", "0", "Offset di paginazione (default 0)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
