// Copyright 2026 vincenzo-vigorito. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newQuantityDiscountsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyApplicaCustomBox bool
	var bodyAttivo bool
	var bodyCategorie string
	var bodyClienti string
	var bodyDataFine string
	var bodyDataInizio string
	var bodyListe string
	var bodyListini string
	var bodyNome string
	var bodyPriorita int
	var bodyProdotti string
	var bodyScaglioni string
	var bodyTargetProdotti string
	var bodyTargetTipoCliente string
	var bodyTargetUtenti string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Serve almeno uno scaglione. Gli id di prodotti, categorie, listini, clienti e liste devono esistere, altrimenti 400...",
		Example:     "  swerpicommerce-pp-cli quantity-discounts create --nome example-value",
		Annotations: map[string]string{"pp:endpoint": "quantity-discounts.create", "pp:method": "POST", "pp:path": "/quantity-discounts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("nome") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "nome")
				}
				if !cmd.Flags().Changed("scaglioni") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "scaglioni")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/quantity-discounts"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyApplicaCustomBox != false {
					body["applica_custom_box"] = bodyApplicaCustomBox
				}
				if bodyAttivo != false {
					body["attivo"] = bodyAttivo
				}
				if bodyCategorie != "" {
					var parsedCategorie any
					if err := json.Unmarshal([]byte(bodyCategorie), &parsedCategorie); err != nil {
						return fmt.Errorf("parsing --categorie JSON: %w", err)
					}
					body["categorie"] = parsedCategorie
				}
				if bodyClienti != "" {
					var parsedClienti any
					if err := json.Unmarshal([]byte(bodyClienti), &parsedClienti); err != nil {
						return fmt.Errorf("parsing --clienti JSON: %w", err)
					}
					body["clienti"] = parsedClienti
				}
				if bodyDataFine != "" {
					body["data_fine"] = bodyDataFine
				}
				if bodyDataInizio != "" {
					body["data_inizio"] = bodyDataInizio
				}
				if bodyListe != "" {
					var parsedListe any
					if err := json.Unmarshal([]byte(bodyListe), &parsedListe); err != nil {
						return fmt.Errorf("parsing --liste JSON: %w", err)
					}
					body["liste"] = parsedListe
				}
				if bodyListini != "" {
					var parsedListini any
					if err := json.Unmarshal([]byte(bodyListini), &parsedListini); err != nil {
						return fmt.Errorf("parsing --listini JSON: %w", err)
					}
					body["listini"] = parsedListini
				}
				if bodyNome != "" {
					body["nome"] = bodyNome
				}
				if bodyPriorita != 0 {
					body["priorita"] = bodyPriorita
				}
				if bodyProdotti != "" {
					var parsedProdotti any
					if err := json.Unmarshal([]byte(bodyProdotti), &parsedProdotti); err != nil {
						return fmt.Errorf("parsing --prodotti JSON: %w", err)
					}
					body["prodotti"] = parsedProdotti
				}
				if bodyScaglioni != "" {
					var parsedScaglioni any
					if err := json.Unmarshal([]byte(bodyScaglioni), &parsedScaglioni); err != nil {
						return fmt.Errorf("parsing --scaglioni JSON: %w", err)
					}
					body["scaglioni"] = parsedScaglioni
				}
				if bodyTargetProdotti != "" {
					body["target_prodotti"] = bodyTargetProdotti
				}
				if bodyTargetTipoCliente != "" {
					body["target_tipo_cliente"] = bodyTargetTipoCliente
				}
				if bodyTargetUtenti != "" {
					body["target_utenti"] = bodyTargetUtenti
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "quantity-discounts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyApplicaCustomBox, "applica-custom-box", false, "Se true la regola vale anche per i prodotti scelti dentro un custom box (righe carrello con custombox_id); se false...")
	cmd.Flags().BoolVar(&bodyAttivo, "attivo", false, "Regola applicata o sospesa. Default in creazione: true.")
	cmd.Flags().StringVar(&bodyCategorie, "categorie", "", "Categorie nominate dalla regola; ognuna si porta dietro le proprie sottocategorie. Con `escluso: true` la categoria...")
	cmd.Flags().StringVar(&bodyClienti, "clienti", "", "Id dei clienti destinatari, usati solo con target_utenti='specifici'")
	cmd.Flags().StringVar(&bodyDataFine, "data-fine", "", "YYYY-MM-DD; null = valida per sempre")
	cmd.Flags().StringVar(&bodyDataInizio, "data-inizio", "", "YYYY-MM-DD; null = valida da sempre")
	cmd.Flags().StringVar(&bodyListe, "liste", "", "Id delle liste (email-lists) i cui iscritti ricevono lo sconto, usate solo con target_utenti='gruppo'")
	cmd.Flags().StringVar(&bodyListini, "listini", "", "Id dei listini su cui la regola vale. Vuoto o assente = tutti i listini.")
	cmd.Flags().StringVar(&bodyNome, "nome", "", "Nome della regola, mostrato solo in amministrazione")
	cmd.Flags().IntVar(&bodyPriorita, "priorita", 0, "A parità di scaglione vince la priorità più alta. Default in creazione: 0.")
	cmd.Flags().StringVar(&bodyProdotti, "prodotti", "", "Prodotti nominati dalla regola. Con `escluso: true` il prodotto viene tolto dall'insieme, anche quando...")
	cmd.Flags().StringVar(&bodyScaglioni, "scaglioni", "", "Soglie della regola. Due scaglioni non possono partire dalla stessa quantità.")
	cmd.Flags().StringVar(&bodyTargetProdotti, "target-prodotti", "", "Insieme di partenza. Default in creazione: tutti.")
	cmd.Flags().StringVar(&bodyTargetTipoCliente, "target-tipo-cliente", "", "Obbligatorio con target_utenti='tipo', ignorato altrimenti")
	cmd.Flags().StringVar(&bodyTargetUtenti, "target-utenti", "", "Chi riceve lo sconto: tutti, i clienti elencati in `clienti`, gli iscritti alle liste in `liste`, oppure il tipo in...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
