// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newArticleCategoriesArticleCategoryCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAttiva bool
	var bodyCategoriaPadreId int
	var bodyDescrizione string
	var bodyImmagine string
	var bodyKeywords string
	var bodyLang string
	var bodyLlmsDescription string
	var bodyLlmsIndex bool
	var bodyMarkups string
	var bodyMetaDescription string
	var bodyMetaTitle string
	var bodyNome string
	var bodyPosizione int
	var bodySlug string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "article-category-create",
		Aliases:     []string{"create"},
		Short:       "Se `slug` manca viene generato dal nome (univoco per lingua).",
		Example:     "  swerpicommerce-pp-cli article-categories article-category-create --nome example-value",
		Annotations: map[string]string{"pp:endpoint": "article-categories.article-category-create", "pp:method": "POST", "pp:path": "/article-categories"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("nome") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "nome")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/article-categories"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAttiva != false {
					body["attiva"] = bodyAttiva
				}
				if bodyCategoriaPadreId != 0 {
					body["categoria_padre_id"] = bodyCategoriaPadreId
				}
				if bodyDescrizione != "" {
					body["descrizione"] = bodyDescrizione
				}
				if bodyImmagine != "" {
					body["immagine"] = bodyImmagine
				}
				if bodyKeywords != "" {
					body["keywords"] = bodyKeywords
				}
				if bodyLang != "" {
					body["lang"] = bodyLang
				}
				if bodyLlmsDescription != "" {
					body["llms_description"] = bodyLlmsDescription
				}
				if bodyLlmsIndex != false {
					body["llms_index"] = bodyLlmsIndex
				}
				if bodyMarkups != "" {
					body["markups"] = bodyMarkups
				}
				if bodyMetaDescription != "" {
					body["meta_description"] = bodyMetaDescription
				}
				if bodyMetaTitle != "" {
					body["meta_title"] = bodyMetaTitle
				}
				if bodyNome != "" {
					body["nome"] = bodyNome
				}
				if bodyPosizione != 0 {
					body["posizione"] = bodyPosizione
				}
				if bodySlug != "" {
					body["slug"] = bodySlug
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "article-categories",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAttiva, "attiva", true, "Attiva")
	cmd.Flags().IntVar(&bodyCategoriaPadreId, "categoria-padre-id", 0, "Categoria padre id")
	cmd.Flags().StringVar(&bodyDescrizione, "descrizione", "", "Descrizione")
	cmd.Flags().StringVar(&bodyImmagine, "immagine", "", "Immagine")
	cmd.Flags().StringVar(&bodyKeywords, "keywords", "", "Keywords")
	cmd.Flags().StringVar(&bodyLang, "lang", "it", "Codice lingua della categoria articoli (es. it, en; default = lingua predefinita del sito). Slug univoco per lingua....")
	cmd.Flags().StringVar(&bodyLlmsDescription, "llms-description", "", "Llms description")
	cmd.Flags().BoolVar(&bodyLlmsIndex, "llms-index", false, "Llms index")
	cmd.Flags().StringVar(&bodyMarkups, "markups", "", "JSON-LD (ItemList)")
	cmd.Flags().StringVar(&bodyMetaDescription, "meta-description", "", "Meta description SEO (nelle risorse products/categories/pages il campo equivalente si chiama 'description')")
	cmd.Flags().StringVar(&bodyMetaTitle, "meta-title", "", "Meta title")
	cmd.Flags().StringVar(&bodyNome, "nome", "", "Nome")
	cmd.Flags().IntVar(&bodyPosizione, "posizione", 0, "Posizione")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Se assente viene generato dal nome (univoco per lingua)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
