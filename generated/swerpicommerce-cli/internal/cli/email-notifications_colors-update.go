// Copyright 2026 vincenzo-vigorito. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newEmailNotificationsColorsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodySwMailBgFooter string
	var bodySwMailBgHeader string
	var bodySwMailBgPulsanteCta string
	var bodySwMailBgPulsanteVuoto string
	var bodySwMailBgSezioniColorate string
	var bodySwMailBordoPulsanteCta string
	var bodySwMailBordoPulsanteVuoto string
	var bodySwMailTextFooter string
	var bodySwMailTextPulsanteCta string
	var bodySwMailTextPulsanteVuoto string
	var bodySwMailTextSezioniColorate string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "colors-update",
		Short:       "Aggiornamento parziale: solo le chiavi passate cambiano. Valori hex (`#RGB` o `#RRGGBB`, normalizzati a `#rrggbb`)....",
		Example:     "  swerpicommerce-pp-cli email-notifications colors-update",
		Annotations: map[string]string{"pp:endpoint": "email-notifications.colors-update", "pp:method": "PUT", "pp:path": "/email-notifications/colors"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/email-notifications/colors"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodySwMailBgFooter != "" {
					body["sw_mail_bg_footer"] = bodySwMailBgFooter
				}
				if bodySwMailBgHeader != "" {
					body["sw_mail_bg_header"] = bodySwMailBgHeader
				}
				if bodySwMailBgPulsanteCta != "" {
					body["sw_mail_bg_pulsante_cta"] = bodySwMailBgPulsanteCta
				}
				if bodySwMailBgPulsanteVuoto != "" {
					body["sw_mail_bg_pulsante_vuoto"] = bodySwMailBgPulsanteVuoto
				}
				if bodySwMailBgSezioniColorate != "" {
					body["sw_mail_bg_sezioni_colorate"] = bodySwMailBgSezioniColorate
				}
				if bodySwMailBordoPulsanteCta != "" {
					body["sw_mail_bordo_pulsante_cta"] = bodySwMailBordoPulsanteCta
				}
				if bodySwMailBordoPulsanteVuoto != "" {
					body["sw_mail_bordo_pulsante_vuoto"] = bodySwMailBordoPulsanteVuoto
				}
				if bodySwMailTextFooter != "" {
					body["sw_mail_text_footer"] = bodySwMailTextFooter
				}
				if bodySwMailTextPulsanteCta != "" {
					body["sw_mail_text_pulsante_cta"] = bodySwMailTextPulsanteCta
				}
				if bodySwMailTextPulsanteVuoto != "" {
					body["sw_mail_text_pulsante_vuoto"] = bodySwMailTextPulsanteVuoto
				}
				if bodySwMailTextSezioniColorate != "" {
					body["sw_mail_text_sezioni_colorate"] = bodySwMailTextSezioniColorate
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "email-notifications",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodySwMailBgFooter, "sw-mail-bg-footer", "", "Sfondo footer")
	cmd.Flags().StringVar(&bodySwMailBgHeader, "sw-mail-bg-header", "", "Sfondo header")
	cmd.Flags().StringVar(&bodySwMailBgPulsanteCta, "sw-mail-bg-pulsante-cta", "", "Sfondo pulsante CTA pieno")
	cmd.Flags().StringVar(&bodySwMailBgPulsanteVuoto, "sw-mail-bg-pulsante-vuoto", "", "Sfondo pulsante outline")
	cmd.Flags().StringVar(&bodySwMailBgSezioniColorate, "sw-mail-bg-sezioni-colorate", "", "Sfondo sezioni colorate (riepiloghi, box dati)")
	cmd.Flags().StringVar(&bodySwMailBordoPulsanteCta, "sw-mail-bordo-pulsante-cta", "", "Bordo pulsante CTA pieno")
	cmd.Flags().StringVar(&bodySwMailBordoPulsanteVuoto, "sw-mail-bordo-pulsante-vuoto", "", "Bordo pulsante outline")
	cmd.Flags().StringVar(&bodySwMailTextFooter, "sw-mail-text-footer", "", "Testo footer")
	cmd.Flags().StringVar(&bodySwMailTextPulsanteCta, "sw-mail-text-pulsante-cta", "", "Testo pulsante CTA pieno")
	cmd.Flags().StringVar(&bodySwMailTextPulsanteVuoto, "sw-mail-text-pulsante-vuoto", "", "Testo pulsante outline")
	cmd.Flags().StringVar(&bodySwMailTextSezioniColorate, "sw-mail-text-sezioni-colorate", "", "Testo sezioni colorate")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
