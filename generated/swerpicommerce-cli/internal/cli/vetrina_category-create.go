// Copyright 2026 vincenzo-vigorito. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newVetrinaCategoryCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAttiva bool
	var bodyCategoriaPadreId int
	var bodyDescription string
	var bodyDescrizione string
	var bodyDescrizioneBreve string
	var bodyFollow bool
	var bodyImmagine string
	var bodyImmagineAlt string
	var bodyInEvidenza bool
	var bodyIndex bool
	var bodyKeywords string
	var bodyLang string
	var bodyMetaTitle string
	var bodyNome string
	var bodyOrdinamento int
	var bodySlug string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "category-create",
		Short:       "Se `slug` manca viene generato dal nome; è unico tra le categorie con lo stesso padre e la stessa lingua (suffisso...",
		Example:     "  swerpicommerce-pp-cli vetrina category-create --nome example-value",
		Annotations: map[string]string{"pp:endpoint": "vetrina.category-create", "pp:method": "POST", "pp:path": "/vetrina/categories"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("nome") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "nome")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/vetrina/categories"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAttiva != false {
					body["attiva"] = bodyAttiva
				}
				if bodyCategoriaPadreId != 0 {
					body["categoria_padre_id"] = bodyCategoriaPadreId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDescrizione != "" {
					body["descrizione"] = bodyDescrizione
				}
				if bodyDescrizioneBreve != "" {
					body["descrizione_breve"] = bodyDescrizioneBreve
				}
				if bodyFollow != false {
					body["follow"] = bodyFollow
				}
				if bodyImmagine != "" {
					body["immagine"] = bodyImmagine
				}
				if bodyImmagineAlt != "" {
					body["immagine_alt"] = bodyImmagineAlt
				}
				if bodyInEvidenza != false {
					body["in_evidenza"] = bodyInEvidenza
				}
				if bodyIndex != false {
					body["index"] = bodyIndex
				}
				if bodyKeywords != "" {
					body["keywords"] = bodyKeywords
				}
				if bodyLang != "" {
					body["lang"] = bodyLang
				}
				if bodyMetaTitle != "" {
					body["meta_title"] = bodyMetaTitle
				}
				if bodyNome != "" {
					body["nome"] = bodyNome
				}
				if bodyOrdinamento != 0 {
					body["ordinamento"] = bodyOrdinamento
				}
				if bodySlug != "" {
					body["slug"] = bodySlug
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "vetrina",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAttiva, "attiva", false, "Se false la categoria, le sue sottocategorie e i loro prodotti non compaiono sul sito (404). Default in creazione: true.")
	cmd.Flags().IntVar(&bodyCategoriaPadreId, "categoria-padre-id", 0, "Categoria padre (null = primo livello). Profondità libera. La categoria stessa o un suo discendente -> 400...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Meta description SEO")
	cmd.Flags().StringVar(&bodyDescrizione, "descrizione", "", "HTML mostrato nella pagina della categoria, sotto la banda titolo. Indentalo in modo ordinato e leggibile.")
	cmd.Flags().StringVar(&bodyDescrizioneBreve, "descrizione-breve", "", "Una riga di testo semplice: nelle card della radice del catalogo e nella banda titolo della categoria.")
	cmd.Flags().BoolVar(&bodyFollow, "follow", false, "Default in creazione: true.")
	cmd.Flags().StringVar(&bodyImmagine, "immagine", "", "Nome file in /uploads/vetrina/categorie_img/ (valore_campo di GET /media, folder vetrina_categorie); file assente ->...")
	cmd.Flags().StringVar(&bodyImmagineAlt, "immagine-alt", "", "Testo alternativo dell'immagine (SEO/accessibilità)")
	cmd.Flags().BoolVar(&bodyInEvidenza, "in-evidenza", false, "Linea di punta: nella radice del catalogo (e nella home d'esempio) ha la card grande con l'elenco delle...")
	cmd.Flags().BoolVar(&bodyIndex, "index", false, "Default in creazione: true.")
	cmd.Flags().StringVar(&bodyKeywords, "keywords", "", "Parole chiave SEO")
	cmd.Flags().StringVar(&bodyLang, "lang", "", "Codice lingua (es. it, en). Ogni traduzione è una categoria separata. Default in creazione: lingua predefinita del...")
	cmd.Flags().StringVar(&bodyMetaTitle, "meta-title", "", "Titolo SEO della pagina categoria")
	cmd.Flags().StringVar(&bodyNome, "nome", "", "Nome")
	cmd.Flags().IntVar(&bodyOrdinamento, "ordinamento", 0, "Posizione tra le sorelle. Default in creazione: 0.")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Se assente viene generato dal nome; unico tra le categorie con lo stesso padre e lingua (suffisso -2, -3 in caso di...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
