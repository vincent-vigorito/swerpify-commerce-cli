// Copyright 2026 Vincenzo Vigorito and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newExtraTabsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAttivo bool
	var bodyCategorie string
	var bodyHtmlContent string
	var bodyLang string
	var bodyNome string
	var bodyNomeInterno string
	var bodyOrdine int
	var bodyProdotti string
	var bodySlug string
	var bodyTargetProdotti string
	var bodyTipo string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "`slug` omesso -> ricavato dal `nome`; deve essere unico per lingua (409 EXTRA_TAB_DUPLICATE_SLUG).",
		Annotations: map[string]string{"pp:endpoint": "extra-tabs.create", "pp:method": "POST", "pp:path": "/extra-tabs", "pp:requires-input": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("nome") && bodyNome == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "nome")
				}
			}
			path := "/extra-tabs"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("attivo") {
					bodyMap["attivo"] = bodyAttivo
				}
				if cmd.Flags().Changed("categorie") || bodyCategorie != "" {
					var parsedCategorie any
					if err := json.Unmarshal([]byte(bodyCategorie), &parsedCategorie); err != nil {
						return fmt.Errorf("parsing --categorie JSON: %w", err)
					}
					asArray, ok := parsedCategorie.([]any)
					if !ok {
						return fmt.Errorf("--categorie must be a JSON array, got JSON %T", parsedCategorie)
					}
					bodyMap["categorie"] = asArray
				}
				if cmd.Flags().Changed("html-content") || bodyHtmlContent != "" {
					bodyMap["html_content"] = bodyHtmlContent
				}
				if cmd.Flags().Changed("lang") || bodyLang != "" {
					bodyMap["lang"] = bodyLang
				}
				if cmd.Flags().Changed("nome") || bodyNome != "" {
					bodyMap["nome"] = bodyNome
				}
				if cmd.Flags().Changed("nome-interno") || bodyNomeInterno != "" {
					bodyMap["nome_interno"] = bodyNomeInterno
				}
				if cmd.Flags().Changed("ordine") || bodyOrdine != 0 {
					bodyMap["ordine"] = bodyOrdine
				}
				if cmd.Flags().Changed("prodotti") || bodyProdotti != "" {
					var parsedProdotti any
					if err := json.Unmarshal([]byte(bodyProdotti), &parsedProdotti); err != nil {
						return fmt.Errorf("parsing --prodotti JSON: %w", err)
					}
					asArray, ok := parsedProdotti.([]any)
					if !ok {
						return fmt.Errorf("--prodotti must be a JSON array, got JSON %T", parsedProdotti)
					}
					bodyMap["prodotti"] = asArray
				}
				if cmd.Flags().Changed("slug") || bodySlug != "" {
					bodyMap["slug"] = bodySlug
				}
				if cmd.Flags().Changed("target-prodotti") || bodyTargetProdotti != "" {
					bodyMap["target_prodotti"] = bodyTargetProdotti
				}
				if cmd.Flags().Changed("tipo") || bodyTipo != "" {
					bodyMap["tipo"] = bodyTipo
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "extra-tabs", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "extra-tabs", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "extra-tabs", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "extra-tabs", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "extra-tabs", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "extra-tabs",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				var selectErr error
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, nil)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "extra-tabs", partialFailure.Message))
				}
				return selectErr
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlagsMeta renders the body with live
			// provenance, then the typed partial-failure exit fires unless
			// --allow-partial-failure downgrades it. Without this guard a
			// partial failure would exit 0 for these output modes — the exact
			// silent-swallow regression the surrounding patch is preventing
			// for asJSON / piped output.
			printErr := printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, nil)
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "extra-tabs", partialFailure.Message))
			}
			return printErr
		},
	}
	cmd.Flags().BoolVar(&bodyAttivo, "attivo", false, "Default in creazione: true.")
	cmd.Flags().StringVar(&bodyCategorie, "categorie", "", "Categorie incluse (target_prodotti=categorie, sottocategorie comprese) o escluse (`escluso: true`).")
	cmd.Flags().StringVar(&bodyHtmlContent, "html-content", "", "Contenuto HTML, usato solo se tipo=generale.")
	cmd.Flags().StringVar(&bodyLang, "lang", "", "Lingua dei prodotti su cui vale (i prodotti sono per-lingua). Default in creazione: it.")
	cmd.Flags().StringVar(&bodyNome, "nome", "", "Titolo del tab mostrato in vetrina; puo' ripetersi fra tab diversi (es. «Specifiche»)")
	cmd.Flags().StringVar(&bodyNomeInterno, "nome-interno", "", "Nome per il pannello, per distinguere tab con lo stesso titolo (es. «Datterino rosso - Specifiche»).")
	cmd.Flags().IntVar(&bodyOrdine, "ordine", 0, "Posizione fra i tab extra, crescente. Default in creazione: 0.")
	cmd.Flags().StringVar(&bodyProdotti, "prodotti", "", "Prodotti inclusi (target_prodotti=prodotti) o esclusi (`escluso: true`, con tutti/categorie).")
	cmd.Flags().StringVar(&bodySlug, "slug", "", "Chiave stabile (id DOM `tab-extra-<slug>`), unica per lingua. Se omessa si ricava dal nome interno, o dal nome.")
	cmd.Flags().StringVar(&bodyTargetProdotti, "target-prodotti", "", "Insieme di partenza dell'ambito. Default in creazione: tutti.")
	cmd.Flags().StringVar(&bodyTipo, "tipo", "", "generale = un HTML per tutti i prodotti dell'ambito; specifico = HTML scritto prodotto per prodotto.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
