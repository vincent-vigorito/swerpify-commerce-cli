// Copyright 2026 Vincenzo Vigorito and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
	"swerpicommerce-pp-cli/internal/cliutil"
)

func newCustomersUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyCap string
	var bodyCellulareAziendale string
	var bodyCf string
	var bodyCitta string
	var bodyCognome string
	var bodyDataNascita string
	var bodyEmail string
	var bodyEmailAziendale string
	var bodyIndirizzo string
	var bodyIndirizzo2 string
	var bodyIva float64
	var bodyLang string
	var bodyListaEmailId int
	var bodyListinoId int
	var bodyNazione string
	var bodyNome string
	var bodyPassword string
	var bodyPec string
	var bodyPiva string
	var bodyPrefissoTelefono string
	var bodyProvincia string
	var bodyRagioneSociale string
	var bodySdi string
	var bodyTags string
	var bodyTelefono string
	var bodyTipo string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <id>",
		Short:       "Aggiorna i campi indicati. `email` e `password` agiscono sull'account di login collegato (l'email deve restare univoca).",
		Annotations: map[string]string{"pp:endpoint": "customers.update", "pp:method": "PUT", "pp:path": "/customers/{id}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <id>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <id>"))
			}
			if !stdinBody {
			}
			path := "/customers/{id}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("id is required\nUsage: %s <%s>", cmd.CommandPath(), "id"))
			}
			path = replacePathParam(path, "id", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("cap") || bodyCap != "" {
					bodyMap["cap"] = bodyCap
				}
				if cmd.Flags().Changed("cellulare-aziendale") || bodyCellulareAziendale != "" {
					bodyMap["cellulare_aziendale"] = bodyCellulareAziendale
				}
				if cmd.Flags().Changed("cf") || bodyCf != "" {
					bodyMap["cf"] = bodyCf
				}
				if cmd.Flags().Changed("citta") || bodyCitta != "" {
					bodyMap["citta"] = bodyCitta
				}
				if cmd.Flags().Changed("cognome") || bodyCognome != "" {
					bodyMap["cognome"] = bodyCognome
				}
				if cmd.Flags().Changed("data-nascita") || bodyDataNascita != "" {
					bodyMap["data_nascita"] = bodyDataNascita
				}
				if cmd.Flags().Changed("email") || bodyEmail != "" {
					bodyMap["email"] = bodyEmail
				}
				if cmd.Flags().Changed("email-aziendale") || bodyEmailAziendale != "" {
					bodyMap["email_aziendale"] = bodyEmailAziendale
				}
				if cmd.Flags().Changed("indirizzo") || bodyIndirizzo != "" {
					bodyMap["indirizzo"] = bodyIndirizzo
				}
				if cmd.Flags().Changed("indirizzo-2") || bodyIndirizzo2 != "" {
					bodyMap["indirizzo_2"] = bodyIndirizzo2
				}
				if cmd.Flags().Changed("iva") || bodyIva != 0.0 {
					bodyMap["iva"] = bodyIva
				}
				if cmd.Flags().Changed("lang") || bodyLang != "" {
					bodyMap["lang"] = bodyLang
				}
				if cmd.Flags().Changed("lista-email-id") || bodyListaEmailId != 0 {
					bodyMap["lista_email_id"] = bodyListaEmailId
				}
				if cmd.Flags().Changed("listino-id") || bodyListinoId != 0 {
					bodyMap["listino_id"] = bodyListinoId
				}
				if cmd.Flags().Changed("nazione") || bodyNazione != "" {
					bodyMap["nazione"] = bodyNazione
				}
				if cmd.Flags().Changed("nome") || bodyNome != "" {
					bodyMap["nome"] = bodyNome
				}
				if cmd.Flags().Changed("password") || bodyPassword != "" {
					bodyMap["password"] = bodyPassword
				}
				if cmd.Flags().Changed("pec") || bodyPec != "" {
					bodyMap["pec"] = bodyPec
				}
				if cmd.Flags().Changed("piva") || bodyPiva != "" {
					bodyMap["piva"] = bodyPiva
				}
				if cmd.Flags().Changed("prefisso-telefono") || bodyPrefissoTelefono != "" {
					bodyMap["prefisso_telefono"] = bodyPrefissoTelefono
				}
				if cmd.Flags().Changed("provincia") || bodyProvincia != "" {
					bodyMap["provincia"] = bodyProvincia
				}
				if cmd.Flags().Changed("ragione-sociale") || bodyRagioneSociale != "" {
					bodyMap["ragione_sociale"] = bodyRagioneSociale
				}
				if cmd.Flags().Changed("sdi") || bodySdi != "" {
					bodyMap["sdi"] = bodySdi
				}
				if cmd.Flags().Changed("tags") {
					parsedTags, parseErr := cliutil.ParseStringList(bodyTags)
					if parseErr != nil {
						return fmt.Errorf("parsing --tags list: %w", parseErr)
					}
					bodyMap["tags"] = parsedTags
				}
				if cmd.Flags().Changed("telefono") || bodyTelefono != "" {
					bodyMap["telefono"] = bodyTelefono
				}
				if cmd.Flags().Changed("tipo") || bodyTipo != "" {
					bodyMap["tipo"] = bodyTipo
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "customers", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "customers", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "customers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				var selectErr error
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, nil)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
				}
				return selectErr
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlagsMeta renders the body with live
			// provenance, then the typed partial-failure exit fires unless
			// --allow-partial-failure downgrades it. Without this guard a
			// partial failure would exit 0 for these output modes — the exact
			// silent-swallow regression the surrounding patch is preventing
			// for asJSON / piped output.
			printErr := printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, nil)
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "customers", partialFailure.Message))
			}
			return printErr
		},
	}
	cmd.Flags().StringVar(&bodyCap, "cap", "", "Cap")
	cmd.Flags().StringVar(&bodyCellulareAziendale, "cellulare-aziendale", "", "Cellulare aziendale")
	cmd.Flags().StringVar(&bodyCf, "cf", "", "Cf")
	cmd.Flags().StringVar(&bodyCitta, "citta", "", "Citta")
	cmd.Flags().StringVar(&bodyCognome, "cognome", "", "Cognome")
	cmd.Flags().StringVar(&bodyDataNascita, "data-nascita", "", "Data nascita")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "Email")
	cmd.Flags().StringVar(&bodyEmailAziendale, "email-aziendale", "", "Email aziendale")
	cmd.Flags().StringVar(&bodyIndirizzo, "indirizzo", "", "Indirizzo")
	cmd.Flags().StringVar(&bodyIndirizzo2, "indirizzo-2", "", "Interno, scala, ecc.")
	cmd.Flags().Float64Var(&bodyIva, "iva", 0.0, "Iva")
	cmd.Flags().StringVar(&bodyLang, "lang", "", "Lang")
	cmd.Flags().IntVar(&bodyListaEmailId, "lista-email-id", 0, "Lista email principale del cliente (null -> lista di default)")
	cmd.Flags().IntVar(&bodyListinoId, "listino-id", 0, "Listino id")
	cmd.Flags().StringVar(&bodyNazione, "nazione", "", "Nazione")
	cmd.Flags().StringVar(&bodyNome, "nome", "", "Nome")
	cmd.Flags().StringVar(&bodyPassword, "password", "", "Password")
	cmd.Flags().StringVar(&bodyPec, "pec", "", "Pec")
	cmd.Flags().StringVar(&bodyPiva, "piva", "", "Piva")
	cmd.Flags().StringVar(&bodyPrefissoTelefono, "prefisso-telefono", "", "Prefisso telefono")
	cmd.Flags().StringVar(&bodyProvincia, "provincia", "", "Provincia")
	cmd.Flags().StringVar(&bodyRagioneSociale, "ragione-sociale", "", "Ragione sociale")
	cmd.Flags().StringVar(&bodySdi, "sdi", "", "Sdi")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Sostituisce l'insieme dei tag del cliente (nomi, case-insensitive; i tag mancanti vengono creati).")
	cmd.Flags().StringVar(&bodyTelefono, "telefono", "", "Telefono")
	cmd.Flags().StringVar(&bodyTipo, "tipo", "", "Tipo")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
