// Copyright 2026 Vincenzo Vigorito and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"bytes"

	"encoding/json"
	"errors"
	"fmt"
	"os"
	"strings"
	"testing"

	"swerpicommerce-pp-cli/internal/cliutil"

	"github.com/spf13/cobra"
	"swerpicommerce-pp-cli/internal/cliutil/testenv"
)

func TestMain(m *testing.M) {
	os.Exit(testenv.RunSandboxed(m))
}

func TestDeclaredAPISurfaceReachable(t *testing.T) {
	expected := []string{
		"article-categories",
		"article-categories article-category-create",
		"article-categories article-category-delete",
		"article-categories article-category-get",
		"article-categories article-category-update",
		"article-categories list",
		"articles",
		"articles authors-list",
		"articles create",
		"articles delete",
		"articles get",
		"articles list",
		"articles update",
		"attributes",
		"attributes create",
		"attributes delete",
		"attributes get",
		"attributes list",
		"attributes update",
		"attributes values",
		"attributes values attribute-create",
		"attributes values attribute-delete",
		"attributes values attribute-update",
		"automation-settings",
		"automation-settings get",
		"automation-settings update",
		"automations",
		"automations create",
		"automations delete",
		"automations executions",
		"automations executions automation-list",
		"automations get",
		"automations list",
		"automations lookups-get",
		"automations run",
		"automations run automation",
		"automations test",
		"automations test automation",
		"automations update",
		"back-in-stock-requests",
		"back-in-stock-requests delete",
		"back-in-stock-requests list",
		"back-in-stock-requests notify",
		"brands",
		"brands create",
		"brands delete",
		"brands get",
		"brands list",
		"brands update",
		"campaigns",
		"campaigns create",
		"campaigns delete",
		"campaigns get",
		"campaigns list",
		"campaigns send",
		"campaigns send campaign",
		"campaigns stats",
		"campaigns stats campaign",
		"campaigns update",
		"carts",
		"carts get",
		"carts list",
		"categories",
		"categories category-create",
		"categories category-delete",
		"categories category-get",
		"categories category-update",
		"categories list",
		"config",
		"config autocommit-get",
		"config autocommit-update",
		"config back-in-stock-get",
		"config back-in-stock-update",
		"config llms-get",
		"config llms-update",
		"custom-apps",
		"custom-apps create",
		"custom-apps delete",
		"custom-apps errors",
		"custom-apps errors custom-app",
		"custom-apps get",
		"custom-apps list",
		"custom-apps update",
		"custom-apps-guide",
		"customer-tags",
		"customer-tags create",
		"customer-tags delete",
		"customer-tags list",
		"customers",
		"customers batch",
		"customers create",
		"customers delete",
		"customers get",
		"customers list",
		"customers points",
		"customers points customer-adjust",
		"customers points customer-get",
		"customers tags",
		"customers tags customer-add",
		"customers tags customer-remove",
		"customers update",
		"design",
		"design color-create",
		"design color-delete",
		"design color-get",
		"design color-update",
		"design colors-list",
		"design compile",
		"design css-delete",
		"design css-get",
		"design css-list",
		"design css-put",
		"design guide",
		"design js-delete",
		"design js-get",
		"design js-list",
		"design js-put",
		"design logos-get",
		"design logos-update",
		"design template-delete",
		"design template-get",
		"design template-put",
		"design templates-guide",
		"design templates-list",
		"design variables-get",
		"discount-codes",
		"discount-codes create",
		"discount-codes delete",
		"discount-codes get",
		"discount-codes list",
		"discount-codes update",
		"email-lists",
		"email-lists create",
		"email-lists delete",
		"email-lists get",
		"email-lists list",
		"email-lists subscribers",
		"email-lists subscribers add",
		"email-lists subscribers list",
		"email-lists subscribers remove",
		"email-lists update",
		"email-notifications",
		"email-notifications colors-get",
		"email-notifications colors-update",
		"email-notifications footer-get",
		"email-notifications footer-update",
		"email-notifications get",
		"email-notifications list",
		"email-notifications restore",
		"email-notifications restore email-notification",
		"email-notifications update",
		"email-templates",
		"email-templates create",
		"email-templates delete",
		"email-templates get",
		"email-templates list",
		"email-templates update",
		"emails",
		"extra-tabs",
		"extra-tabs create",
		"extra-tabs delete",
		"extra-tabs get",
		"extra-tabs list",
		"extra-tabs update",
		"fonts",
		"fonts assignments-get",
		"fonts assignments-update",
		"fonts create",
		"fonts delete",
		"fonts get",
		"fonts list",
		"fonts update",
		"fork",
		"fork commit",
		"fork diff",
		"fork file-get",
		"fork log",
		"fork restore",
		"fork search",
		"fork version-get",
		"forms",
		"forms create",
		"forms delete",
		"forms get",
		"forms list",
		"forms submissions",
		"forms submissions form-list",
		"forms submissions form-replay",
		"forms update",
		"forms-guide",
		"header-footer",
		"header-footer list",
		"header-footer set",
		"languages",
		"languages create",
		"languages list",
		"legal-settings",
		"legal-settings get",
		"legal-settings list",
		"legal-settings update",
		"maintenance",
		"maintenance get",
		"maintenance update",
		"media",
		"media delete",
		"media get",
		"media list",
		"media update",
		"media upload",
		"orders",
		"orders batch",
		"orders create",
		"orders get",
		"orders list",
		"orders update",
		"page-templates",
		"page-templates assign",
		"page-templates list",
		"pages",
		"pages content",
		"pages content page-get",
		"pages content page-update",
		"pages create",
		"pages delete",
		"pages get",
		"pages list",
		"pages update",
		"payment-methods",
		"payment-methods create",
		"payment-methods delete",
		"payment-methods get",
		"payment-methods list",
		"payment-methods update",
		"price-lists",
		"price-lists get",
		"price-lists list",
		"products",
		"products batch",
		"products create",
		"products delete",
		"products get",
		"products images",
		"products images product-delete",
		"products images product-list",
		"products images product-update",
		"products images product-upload",
		"products list",
		"products stock",
		"products stock product-get",
		"products stock product-update",
		"products update",
		"quantity-discounts",
		"quantity-discounts create",
		"quantity-discounts delete",
		"quantity-discounts get",
		"quantity-discounts list",
		"quantity-discounts update",
		"redirects",
		"redirects create",
		"redirects delete",
		"redirects get",
		"redirects list",
		"redirects update",
		"review-requests",
		"review-requests send",
		"review-requests send review-request",
		"reviews",
		"reviews create",
		"reviews delete",
		"reviews get",
		"reviews list",
		"reviews update",
		"shipping-methods",
		"shipping-methods create",
		"shipping-methods delete",
		"shipping-methods get",
		"shipping-methods list",
		"shipping-methods update",
		"site-cache",
		"site-cache cache-config-update",
		"site-cache cache-flush",
		"site-cache cache-get",
		"site-info",
		"site-specs",
		"site-specs get",
		"site-specs log",
		"site-specs update",
		"swerpicommerce-agent-context",
		"swerpicommerce-auth",
		"swerpicommerce-auth me",
		"swerpicommerce-auth token",
		"swerpicommerce-auth token-revoke",
		"swerpicommerce-auth tokens-list",
		"update",
		"vat-groups",
		"vat-rates",
		"vat-rates get",
		"vat-rates list",
		"vat-rules",
		"vat-rules get",
		"vat-rules update",
		"vat-validations",
		"vetrina",
		"vetrina attribute-create",
		"vetrina attribute-delete",
		"vetrina attribute-get",
		"vetrina attribute-update",
		"vetrina attribute-value-create",
		"vetrina attribute-value-delete",
		"vetrina attribute-value-update",
		"vetrina attributes-list",
		"vetrina categories-list",
		"vetrina category-create",
		"vetrina category-delete",
		"vetrina category-get",
		"vetrina category-update",
		"vetrina product-attachment-delete",
		"vetrina product-attachment-update",
		"vetrina product-attachment-upload",
		"vetrina product-attachments-list",
		"vetrina product-create",
		"vetrina product-delete",
		"vetrina product-get",
		"vetrina product-image-delete",
		"vetrina product-image-update",
		"vetrina product-image-upload",
		"vetrina product-images-list",
		"vetrina product-update",
		"vetrina product-variant-delete",
		"vetrina product-variant-update",
		"vetrina product-variants-generate",
		"vetrina product-variants-list",
		"vetrina products-batch",
		"vetrina products-list",
		"webhooks",
		"webhooks create",
		"webhooks delete",
		"webhooks deliveries",
		"webhooks deliveries webhook-list",
		"webhooks get",
		"webhooks list",
		"webhooks update",
		"well-known",
		"well-known create",
		"well-known delete",
		"well-known get",
		"well-known list",
		"well-known update",
	}
	actual := make(map[string]struct{}, len(expected))
	type pendingCommand struct {
		command *cobra.Command
		path    string
	}
	queue := make([]pendingCommand, 0, len(expected))
	for _, child := range RootCmd().Commands() {
		queue = append(queue, pendingCommand{command: child, path: child.Name()})
	}
	for len(queue) > 0 {
		current := queue[0]
		queue = queue[1:]
		actual[current.path] = struct{}{}
		for _, child := range current.command.Commands() {
			queue = append(queue, pendingCommand{
				command: child,
				path:    strings.TrimSpace(current.path + " " + child.Name()),
			})
		}
	}

	var missing []string
	for _, commandPath := range expected {
		if _, ok := actual[commandPath]; !ok {
			missing = append(missing, commandPath)
		}
	}
	if len(missing) > 0 {
		t.Fatalf("declared API command paths missing from generated Cobra tree: %s", strings.Join(missing, ", "))
	}
}

func TestNoDuplicateCommandNames(t *testing.T) {
	type pendingCommand struct {
		command *cobra.Command
		path    string
	}
	queue := []pendingCommand{}
	queue = append(queue, pendingCommand{command: RootCmd(), path: ""})
	var duplicates []string
	for len(queue) > 0 {
		current := queue[0]
		queue = queue[1:]
		seen := map[string]struct{}{}
		for _, child := range current.command.Commands() {
			childPath := strings.TrimSpace(current.path + " " + child.Name())
			if _, exists := seen[child.Name()]; exists {
				duplicates = append(duplicates, childPath)
			} else {
				seen[child.Name()] = struct{}{}
			}
			queue = append(queue, pendingCommand{command: child, path: childPath})
		}
	}
	if len(duplicates) > 0 {
		t.Fatalf("generated Cobra tree contains duplicate sibling command names: %s", strings.Join(duplicates, ", "))
	}
}
func TestWriteCredentialSaveErrorEnvelope(t *testing.T) {
	var out bytes.Buffer
	cause := &cliutil.CredentialsPermissionError{
		Path: "/tmp/credentials.toml",
		Err:  errors.New("unsafe permissions"),
	}
	if !writeCredentialSaveErrorEnvelope(&out, &rootFlags{asJSON: true}, fmt.Errorf("saving token: %w", cause)) {
		t.Fatal("permission failure envelope was not written")
	}

	var payload struct {
		Saved               bool   `json:"saved"`
		CredentialsPath     string `json:"credentials_path"`
		PermissionsVerified bool   `json:"permissions_verified"`
		Error               string `json:"error"`
		Code                int    `json:"code"`
	}
	if err := json.Unmarshal(out.Bytes(), &payload); err != nil {
		t.Fatalf("permission failure envelope must be valid JSON: %v\n%s", err, out.String())
	}
	if !payload.Saved || payload.CredentialsPath != cause.Path || payload.PermissionsVerified || payload.Error == "" || payload.Code == 0 {
		t.Fatalf("permission failure envelope = %+v, want saved path, unsafe permissions, error, and non-zero code", payload)
	}
}

// TestIsCobraUsageError covers the six pre-RunE error shapes Cobra and
// pflag can produce before any user RunE runs. Each must be detected so
// the caller can wrap in usageErr() and yield exit code 2.
func TestIsCobraUsageError(t *testing.T) {
	t.Parallel()
	cases := []struct {
		name  string
		errIn error
		want  bool
	}{
		{"nil", nil, false},
		{"unknown flag", errors.New("unknown flag: --foob"), true},
		{"unknown shorthand flag", errors.New("unknown shorthand flag: 'x' in -x"), true},
		{"unknown command", errors.New("unknown command \"foo\" for \"cli\""), true},
		{"required flag (singular)", errors.New("required flag \"query\" not set"), true},
		{"required flag(s) (plural)", errors.New("required flag(s) \"query\", \"vault\" not set"), true},
		{"flag needs argument", errors.New("flag needs an argument: --query"), true},
		{"invalid argument", errors.New("invalid argument \"abc\" for \"--limit\" flag: strconv.ParseInt: parsing \"abc\": invalid syntax"), true},
		// Non-usage errors must NOT be flagged — they should retain their
		// original (or wrapped *cliError) exit code.
		{"API error pass-through", errors.New("API returned 500"), false},
		{"network error pass-through", errors.New("dial tcp: connection refused"), false},
		// Pattern-tightness regressions: application RunE errors that
		// happen to mention "required flag" / "flag needs an argument" /
		// "invalid argument" as prose must NOT classify as usage errors.
		// The anchored prefixes guard against this; if the patterns are
		// ever loosened back to Contains() these tests catch the drift.
		{"app msg containing 'required flag' as prose", errors.New("the required flag config is missing from the manifest"), false},
		{"app msg containing 'flag needs an argument' as prose", errors.New("this flag needs an argument upstream before retry"), false},
		{"app msg containing 'invalid argument' as prose", errors.New("invalid argument provided to handler at /foo"), false},
	}
	for _, tc := range cases {
		tc := tc
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()
			if got := isCobraUsageError(tc.errIn); got != tc.want {
				t.Errorf("isCobraUsageError(%v) = %v, want %v", tc.errIn, got, tc.want)
			}
		})
	}
}

// TestIsCobraUsageError_SurvivesHintRewrap covers the hint-suggestion
// path in Execute(): after rewrapping an "unknown flag" error with
// fmt.Errorf("%w\nhint: ...", err, ...), the rewrapped error must still
// classify as a usage error so the exit code stays at 2 rather than
// silently falling through to 1.
func TestIsCobraUsageError_SurvivesHintRewrap(t *testing.T) {
	t.Parallel()
	original := errors.New("unknown flag: --foob")
	wrapped := fmt.Errorf("%w\nhint: did you mean --foo?", original)
	if !isCobraUsageError(wrapped) {
		t.Errorf("hint-rewrapped unknown-flag error must still classify as usage error, got: %v", wrapped)
	}
}

// TestExitCode_UsageError_WrappedAsCode2 covers the end-to-end contract:
// after Execute() wraps a Cobra usage error in usageErr(), ExitCode()
// must return 2, not 1. This is the user-visible promise — a bad flag
// exits 2 (POSIX usage convention), not the generic-error-1 fallback.
func TestExitCode_UsageError_WrappedAsCode2(t *testing.T) {
	t.Parallel()
	wrapped := usageErr(errors.New("unknown flag: --foob"))
	if got := ExitCode(wrapped); got != 2 {
		t.Errorf("ExitCode(usageErr(...)) = %d, want 2 (POSIX usage convention)", got)
	}
}

// TestFilterFields covers --select projection against the four payload
// shapes printed CLIs see in practice: bare arrays, direct objects,
// list envelopes (Stripe/GitHub/Notion-style wrapper + array), and
// flat objects. The envelope cases guard against a regression where
// wrapper-key + array responses returned `{}` because the selector
// heads matched the inner record fields, not the wrapper key.
func TestFilterFields(t *testing.T) {
	t.Parallel()
	cases := []struct {
		name   string
		input  string
		fields string
		want   string
	}{
		{
			name:   "bare array element-wise",
			input:  `[{"id":"a","name":"x","other":"y"},{"id":"b","name":"z","other":"w"}]`,
			fields: "id,name",
			want:   `[{"id":"a","name":"x"},{"id":"b","name":"z"}]`,
		},
		{
			name:   "direct object top-level match",
			input:  `{"id":"a","name":"b","other":"c"}`,
			fields: "id,name",
			want:   `{"id":"a","name":"b"}`,
		},
		{
			name:   "envelope single array sibling (orgo {projects:[...]})",
			input:  `{"projects":[{"id":"a","name":"x","other":"y"}]}`,
			fields: "id,name",
			want:   `{"projects":[{"id":"a","name":"x"}]}`,
		},
		{
			name:   "envelope with metadata sibling (github {total_count,items:[...]})",
			input:  `{"total_count":2,"items":[{"id":"a","name":"x","other":"y"},{"id":"b","name":"z","other":"w"}]}`,
			fields: "id,name",
			want:   `{"items":[{"id":"a","name":"x"},{"id":"b","name":"z"}],"total_count":2}`,
		},
		{
			name:   "envelope with metadata sibling (stripe {object,data:[...]})",
			input:  `{"object":"list","data":[{"id":"a","name":"x","other":"y"}]}`,
			fields: "id,name",
			want:   `{"data":[{"id":"a","name":"x"}],"object":"list"}`,
		},
		{
			name:   "selector matches envelope key (no descent into array)",
			input:  `{"projects":[{"id":"a","name":"x","other":"y"}]}`,
			fields: "projects",
			want:   `{"projects":[{"id":"a","name":"x","other":"y"}]}`,
		},
		{
			name:   "dotted-path through envelope key still descends",
			input:  `{"projects":[{"id":"a","name":"x","other":"y"}]}`,
			fields: "projects.id",
			want:   `{"projects":[{"id":"a"}]}`,
		},
		{
			name:   "flat object no match preserves input",
			input:  `{"a":1,"b":2}`,
			fields: "c",
			want:   `{"a":1,"b":2}`,
		},
		{
			name:   "unknown selector preserves nested array objects",
			input:  `{"items":[{"id":"a","name":"Alpha"},{"id":"b","name":"Beta"}]}`,
			fields: "missing",
			want:   `{"items":[{"id":"a","name":"Alpha"},{"id":"b","name":"Beta"}]}`,
		},
		{
			// Null pagination cursors are common envelope metadata.
			// json.Unmarshal accepts JSON null into []json.RawMessage as
			// a nil slice without error, so the array check must reject
			// nil explicitly or null siblings would be coerced to `[]`.
			name:   "envelope preserves null sibling verbatim",
			input:  `{"items":[{"id":"a","name":"x"}],"next_cursor":null}`,
			fields: "id,name",
			want:   `{"items":[{"id":"a","name":"x"}],"next_cursor":null}`,
		},
		{
			// Without a real array sibling the envelope fallback does not
			// fire, but an invalid selector still preserves the input.
			name:   "flat object with null sibling no match preserves input",
			input:  `{"a":1,"b":null}`,
			fields: "c",
			want:   `{"a":1,"b":null}`,
		},
		{
			// Multiple array siblings at the same level each receive the
			// selector independently. Documents that the projection fans
			// out across every array, not just the first one found.
			name:   "envelope with two array siblings filters both",
			input:  `{"events":[{"id":"e1","other":"x"}],"speakers":[{"id":"s1","other":"y"}]}`,
			fields: "id",
			want:   `{"events":[{"id":"e1"}],"speakers":[{"id":"s1"}]}`,
		},
		{
			// Generic object descent supports type-keyed envelopes such as
			// {"data":{"items":[...]}} while keeping the fail-closed
			// behavior for objects with no collection below them.
			name:   "nested object envelope descends into collection",
			input:  `{"data":{"items":[{"id":"a","other":"y"}]}}`,
			fields: "id",
			want:   `{"data":{"items":[{"id":"a"}]}}`,
		},
	}
	for _, tc := range cases {
		tc := tc
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()
			got := filterFields(json.RawMessage(tc.input), tc.fields)
			// Normalize both sides through json.Unmarshal+Marshal so
			// map-iteration order does not produce false negatives.
			var gotV, wantV interface{}
			if err := json.Unmarshal(got, &gotV); err != nil {
				t.Fatalf("got is invalid json: %v (raw=%s)", err, string(got))
			}
			if err := json.Unmarshal([]byte(tc.want), &wantV); err != nil {
				t.Fatalf("want is invalid json: %v (raw=%s)", err, tc.want)
			}
			gotBytes, _ := json.Marshal(gotV)
			wantBytes, _ := json.Marshal(wantV)
			if string(gotBytes) != string(wantBytes) {
				t.Errorf("filterFields(%q, %q) = %s, want %s",
					tc.input, tc.fields, string(gotBytes), string(wantBytes))
			}
		})
	}
}
