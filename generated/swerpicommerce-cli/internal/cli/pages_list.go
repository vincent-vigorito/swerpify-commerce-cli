// Copyright 2026 Vincenzo Vigorito and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPagesListCmd(flags *rootFlags) *cobra.Command {
	var flagLang string
	var flagSlug string
	var flagHomepage bool
	var flagSitemap bool
	var flagPaginaPadreId string
	var flagPaginaSistema string
	var flagLlmsIndex bool
	var flagIncludeAlternates bool
	var flagLimit int
	var flagOffset string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Le pagine sono template-driven: il contenuto HTML non e un campo del modello ma un template Django per-istanza",
		Example:     "  swerpicommerce-pp-cli pages list",
		Annotations: map[string]string{"pp:endpoint": "pages.list", "pp:method": "GET", "pp:path": "/pages", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("pagina-sistema") {
				allowedPaginaSistema := []string{"blog", "blog-articolo", "blog-categoria", "blog-tag", "blog-search", "custom-box", "negozio", "categoria-prodotto", "carrello", "pagamento", "ordine-completato", "prodotto-singolo", "mio-account", "parco-auto", "auto-singola", "vetrina", "vetrina-categoria", "vetrina-prodotto", "manutenzione", "vacanza"}
				validPaginaSistema := false
				for _, v := range allowedPaginaSistema {
					if flagPaginaSistema == v {
						validPaginaSistema = true
						break
					}
				}
				if !validPaginaSistema {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagPaginaSistema, "pagina-sistema", allowedPaginaSistema)
				}
			}
			path := "/pages"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "pages", path, retainCLIQueryParams(cmd, map[string]string{
				"lang":               formatCLIParamValue(flagLang),
				"slug":               formatCLIParamValue(flagSlug),
				"homepage":           formatCLIParamValue(flagHomepage),
				"sitemap":            formatCLIParamValue(flagSitemap),
				"pagina_padre_id":    formatCLIParamValue(flagPaginaPadreId),
				"pagina_sistema":     formatCLIParamValue(flagPaginaSistema),
				"llms_index":         formatCLIParamValue(flagLlmsIndex),
				"include_alternates": formatCLIParamValue(flagIncludeAlternates),
				"limit":              formatCLIParamValue(flagLimit),
				"offset":             formatCLIParamValue(flagOffset),
			}, map[string][]string{"lang": {"lang"}, "slug": {"slug"}, "homepage": {"homepage"}, "sitemap": {"sitemap"}, "pagina_padre_id": {"pagina-padre-id"}, "pagina_sistema": {"pagina-sistema"}, "llms_index": {"llms-index"}, "include_alternates": {"include-alternates"}, "limit": {"limit"}, "offset": {"offset"}}, "offset", "offset"), nil, flagAll, "offset", "offset", "limit", 100, "", "", "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				var selectErr error
				filtered := data
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, nil)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				if err := printOutput(cmd.OutOrStdout(), wrapped, true); err != nil {
					return err
				}
				return selectErr
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, nil)
		},
	}
	cmd.Flags().StringVar(&flagLang, "lang", "", "Filtra le pagine per lingua (match esatto, nessun fallback). Omesso = tutte le lingue.")
	cmd.Flags().StringVar(&flagSlug, "slug", "", "Slug")
	cmd.Flags().BoolVar(&flagHomepage, "homepage", false, "Homepage")
	cmd.Flags().BoolVar(&flagSitemap, "sitemap", false, "Sitemap")
	cmd.Flags().StringVar(&flagPaginaPadreId, "pagina-padre-id", "", "Pagina padre id")
	cmd.Flags().StringVar(&flagPaginaSistema, "pagina-sistema", "", "Filtra per tipo di pagina di sistema (vedi `SystemPageType`). (one of: blog, blog-articolo, blog-categoria, blog-tag, blog-search, custom-box, negozio, categoria-prodotto, carrello, pagamento, ordine-completato, prodotto-singolo, mio-account, parco-auto, auto-singola, vetrina, vetrina-categoria, vetrina-prodotto, manutenzione, vacanza)")
	cmd.Flags().BoolVar(&flagLlmsIndex, "llms-index", false, "Llms index")
	cmd.Flags().BoolVar(&flagIncludeAlternates, "include-alternates", true, "Include nell'output l'array `alternates` con le versioni multilingua collegate. False per alleggerire la risposta.")
	cmd.Flags().IntVar(&flagLimit, "limit", 100, "Numero massimo di risultati (default 100)")
	cmd.Flags().StringVar(&flagOffset, "offset", "0", "Offset di paginazione (default 0)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
